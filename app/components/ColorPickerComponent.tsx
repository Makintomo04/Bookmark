import { FC } from 'react'
import { SketchPicker } from 'react-color';
interface ColorPickerComponentProps {
  
}

const ColorPickerComponent: FC<ColorPickerComponentProps> = ({}) => {
  return <SketchPicker />
}

export default ColorPickerComponent